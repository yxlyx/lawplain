# Better Auth
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Cloudflare D1 is exposed through the AUTH_DB binding in wrangler.jsonc.
# Create it with: bun run d1:create
